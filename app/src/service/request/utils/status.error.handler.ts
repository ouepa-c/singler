/**
 * Axios错误处理
 * todo
 * */
const statusErrorHandler = (error:any) => {
  // error.response
  // error.response.headers
  // error.response.status // 状态码
  // error.response.data
  // error.request
  // error.message
  // error.config
}

export default statusErrorHandler
